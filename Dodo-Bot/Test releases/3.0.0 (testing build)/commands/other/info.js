module.exports = {
  name: "info",
  code: `#(newEmbed
    #(setTitle regarding canary version)
    #(setDescription this version is mostly used for experimental stuff, do not use this version unless you want to take risks, it's not meant for public use either)
    #(setColor Blurple)
    )`
}
